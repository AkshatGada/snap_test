import { Box, Heading, Button } from "@metamask/snaps-sdk/jsx";

export const Home = () => (
  <Box>
    <Heading>Bridge Helper</Heading>
    <Button name="bridgeBtn">Bridge assets</Button>
  </Box>
);